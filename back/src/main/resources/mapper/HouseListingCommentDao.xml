<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.HouseListingCommentDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.HouseListingCommentEntity" id="houseListingCommentMap">
        <result property="referenceId" column="referenceId"/>
        <result property="userId" column="userId"/>
        <result property="content" column="content"/>
        <result property="reply" column="reply"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.HouseListingCommentVO" >
		SELECT * FROM houseListingComment  houseListingComment         
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.HouseListingCommentVO" >
		SELECT  houseListingComment.* FROM houseListingComment  houseListingComment 	
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.HouseListingCommentView" >

		SELECT  houseListingComment.* FROM houseListingComment  houseListingComment 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.HouseListingCommentView" >
		SELECT * FROM houseListingComment  houseListingComment <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
</mapper>