<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.LandlordApplicationDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.LandlordApplicationEntity" id="landlordApplicationMap">
        <result property="houseName" column="houseName"/>
        <result property="houseType" column="houseType"/>
        <result property="picture" column="picture"/>
        <result property="rentalMode" column="rentalMode"/>
        <result property="orientationFloor" column="orientationFloor"/>
        <result property="community" column="community"/>
        <result property="area" column="area"/>
        <result property="monthlyRent" column="monthlyRent"/>
        <result property="houseDescription" column="houseDescription"/>
        <result property="publishDate" column="publishDate"/>
        <result property="tenantUsername" column="tenantUsername"/>
        <result property="contactPhone" column="contactPhone"/>
        <result property="reviewStatus" column="reviewStatus"/>
        <result property="reviewReply" column="reviewReply"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.LandlordApplicationVO" >
		SELECT * FROM landlordApplication  landlordApplication         
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.LandlordApplicationVO" >
		SELECT  landlordApplication.* FROM landlordApplication  landlordApplication 	
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.LandlordApplicationView" >

		SELECT  landlordApplication.* FROM landlordApplication  landlordApplication 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.LandlordApplicationView" >
		SELECT * FROM landlordApplication  landlordApplication <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
</mapper>